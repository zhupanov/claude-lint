  Final Updated Proposal — 49 Rules, Zero Duplicates

  Context: agnix at /Users/zhupanov/agnix1 branch main@cb3acbe, 399 rules. agent-lint at /Users/zhupanov/agent-lint1, 104 rules. Two successive duplicate-audit
  passes (each with 3 research agents + 3 validation reviewers = 6 expert passes per audit) confirmed zero duplicates among the 49 proposed rules.

  Cross-cutting conventions (apply to every issue):
  - Each new rule requires entries in both knowledge-base/rules.json AND knowledge-base/VALIDATION-RULES.md.
  - Heuristic rules without upstream specs use evidence.source_type: convention, normative_level: recommended.
  - Ported opinionated rules default to warn, not error, pending Issue #8.
  - Rules meaningful only in Claude plugin repos gate on .claude-plugin/ presence (FileType-based activation, matches existing CC-PL-* pattern).
  - Every rule ships with fixtures under tests/fixtures/.

  ---
  Issue #1 — Fix CLAUDE.md line-limit text drift in rules.json

  Why: CC-MEM-014 implementation enforces 200 lines but rules.json text says "500+". Update rules.json to 200. No code change.

  What: Update CC-MEM-014 description/good_example/bad_example and VALIDATION-RULES.md to reference 200 lines.

  Why first: Pure doc fix; good warm-up PR.

  ---
  Issue #2 — Agent description quality rules (3 rules)

  Why: agnix has CC-AG-002 (fires on missing AND empty-after-trim) but no length-bound or redundancy checks.

  What: Extend crates/agnix-core/src/rules/agent.rs:
  - A008 → CC-AG-0XX agent description > 1024 characters — error.
  - A009 → CC-AG-0XX agent description present and non-empty-after-trim but length-after-trim < 20 characters — warn. Implementation note: call .trim() before length
   check to cleanly exclude CC-AG-002's coverage of whitespace-only descriptions.
  - A011 → CC-AG-0XX agent description too similar to name (Jaccard >0.8 on tokenized words with stopwords + token-containment heuristic; mirror agent-lint's
  agents.rs logic) — warn.

  ---
  Issue #3 — CLAUDE.md canonical doc references (1 rule)

  Why: agnix has REF-002 (markdown links) and CC-MEM-004 (invalid /command refs). Neither catches bare path bullets in a canonical-sources section.

  What: Extend crates/agnix-core/src/rules/claude_md.rs:
  - D001 → CC-MEM-0XX CLAUDE.md canonical-docs reference missing on disk — warn. Parses a "Sources"/"Canonical docs"/"References" section and verifies each listed
  path exists. Partial overlap with REF-002 when users write refs as markdown links; acceptable redundancy on that edge case.

  ---
  Issue #4 — Complete userConfig validation in plugin.json (6 rules)

  Why: agnix has only CC-PL-012 (key-name schema). No rule validates entry object shape. Verified at plugin.rs:318-343.

  What: Extend crates/agnix-core/src/rules/plugin.rs:
  - U001 → CC-PL-0XX userConfig must be an object — error.
  - U002 → CC-PL-0XX userConfig entry missing/invalid description field — error.
  - U003 → CC-PL-0XX userConfig key has no corresponding $CLAUDE_PLUGIN_OPTION_<KEY> reference in scripts/**/*.sh — error. Cross-file grounding; follow
  schemas::agents_md / schemas::cross_platform pattern.
  - U004 → CC-PL-0XX userConfig sensitive must be boolean — error.
  - U005 → CC-PL-0XX userConfig entry missing/invalid title — error.
  - U006 → CC-PL-0XX userConfig entry missing/invalid type — error.

  ---
  Issue #5 — Add marketplace.json validator (7 rules)

  Why: agnix has zero marketplace coverage. Verified: grep -i marketplace returns 0 rule hits.

  What:
  - Register FileType::ClaudeMarketplace in crates/agnix-core/src/file_types/, detected at .claude-plugin/marketplace.json. Verify pipeline.rs [files] override
  interaction.
  - Add crates/agnix-core/src/rules/marketplace.rs (new module in ValidatorRegistry).
  - M005 → CC-MP-001 marketplace.json missing (when .claude-plugin/ present) — warn.
  - M006 → CC-MP-002 marketplace.json invalid JSON — error.
  - M007 → CC-MP-003 marketplace missing name or owner.name — error.
  - M008 → CC-MP-004 marketplace plugins array empty — error.
  - M009 → CC-MP-005 marketplace plugin entry invalid name or source — error.
  - M010 → CC-MP-006 marketplace enriched metadata missing (owner.email, plugin category) — warn.
  - M011-partial → CC-PL-0XX plugin.json missing author.email or non-empty keywords — warn. Description already covered by CC-PL-004; author-object-shape by
  CC-PL-009.

  ---
  Issue #6 — Plugin hygiene bundle (10 rules)

  Why: Cohesive hygiene bundle — path hygiene, script existence/executability, dead-script detection, SECURITY.md, TODO markers, email format,
  empty-legacy-hooks-array. All individually verified absent from agnix.

  What: Add crates/agnix-core/src/rules/plugin_hygiene.rs (project-level), activated when .claude-plugin/ is detected:
  - G001 → CC-PH-001 skill content uses $PWD/ or hardcoded path instead of ${CLAUDE_PLUGIN_ROOT}/ — error.
  - G002 → CC-PH-002 script referenced from SKILL.md missing on disk (skill-script scope; distinct from CC-HK-008 hook-script scope) — error.
  - G003 → CC-PH-003 script file not executable (Unix-only) — error.
  - G004 → CC-PH-004 dead script in top-level scripts/ directory (not referenced from any SKILL.md, hooks.json, settings.json, .github/workflows) — warn.
  - G005 → CC-PH-005 SECURITY.md missing at repo root — warn. Note: agnix's file_types/detection.rs:31-48 currently excludes SECURITY.md from markdown validation but
   does not require its presence — this rule adds the presence requirement.
  - G006 → CC-PH-006 TODO/FIXME/HACK/XXX marker in skill body outside fences — warn.
  - G007 → CC-PH-007 TODO/FIXME/HACK/XXX marker in agent .md body — warn.
  - D003 → CC-MEM-0XX TODO/FIXME/HACK/XXX marker in CLAUDE.md body (extend rules/claude_md.rs) — warn.
  - E001 → CC-PH-008 invalid email format in plugin/marketplace JSON string fields (only author.email, owner.email) — error.
  - H007 → CC-HK-0XX empty hooks array in legacy hooks/hooks.json — error. Requires registering new FileType for hooks/hooks.json path (currently falls through to
  FileType::Unknown per detection.rs:366-399).

  Implementation note: G006/G007/D003 share regex helper; G004 and S030 scan different directories (top-level scripts/ vs per-skill skills/{name}/scripts/).

  ---
  Issue #7 — Skill content heuristics (22 rules)

  Why: agent-lint's main differentiator — skill-body and frontmatter heuristics beyond structural validation. All 22 rules individually verified as absent from
  agnix. Gated behind .claude-plugin/ presence.

  What: Add crates/agnix-core/src/rules/skill_content/ submodule (mirroring agent-lint's src/validators/skill_content/ layout). Each rule defaults to warn with
  source_type: convention / normative_level: recommended unless noted.

  Frontmatter / structural (2 rules):
  - S028 plain non-indexed $ARGUMENTS in body without argument-hint — error. Complements CC-SK-012 and CC-SK-016.
  - S044 backticked snake_case tool reference in body near context words without ServerName: prefix — warn. Body-prose scope only; CC-SK-008 is frontmatter-only.

  Body content (13 rules):
  - S021 consecutive bash code fences — warn.
  - S029 shared .md referenced by another shared .md in skill context (graph depth >1) — warn. Distinct from CC-MEM-003 (@imports) and AS-013 (path depth).
  - S030 files in per-skill skills/{name}/scripts/ not referenced from that skill's SKILL.md — error. Distinct from G004 (top-level scripts/).
  - S031 http:// URL in skill body — error. Distinct from MCP-017 (MCP-server URL only).
  - S032 hardcoded-secret heuristic in SKILL.md content — error. No existing secret rule scopes to SKILL.md.
  - S036 referenced .md >100 lines with no ## headings — warn.
  - S037 body >300 lines with no file references — warn. Partial overlap with AS-012 (different threshold + condition; multi-fire intentional).
  - S038 body contains time-sensitive year patterns — warn.
  - S046 body >300 lines without workflow structure markers — warn.
  - S047 body >200 lines without examples/templates — warn, default-suppressed (depends on Issue #8).
  - S048 non-descriptive reference filename (notes.md, misc.md, etc.) — warn.
  - S056 body lists alternatives without stated default — warn.
  - S057 undocumented magic number in code block — warn.

  Script-backed skill quality (3 rules):
  - S051 script-backed skill lacks dependency documentation — warn.
  - S052 script-backed skill lacks verification step — warn.
  - S055 script file lacks error-handling pattern — warn.

  Description heuristics (2 rules):
  - S050 skill description content is vague/generic — warn. Distinct from AS-019 (vague NAMES).
  - S054 description keywords not reflected in body — warn.

  Semantic naming (2 rules):
  - S053 terminology inconsistency (3+ synonym variants) — warn.
  - S049 skill name not in gerund form — default-suppressed (depends on Issue #8). Distinct from AS-019.

  ---
  Issue #8 — Add default-suppressed rule tier (feature, 0 new rules)

  Why: S049 and S047 in Issue #7 are too opinionated to ship as default-warn. agent-lint has 3 default-severity tiers (error/warn/suppressed); agnix currently has 2.

  What: Extend severity model in crates/agnix-core/src/config.rs:
  - Add Severity::Suppressed variant (rule defined but does not fire unless explicitly enabled in .agnix.toml).
  - Update rule loading from rules.json to respect a new "default_state": "suppressed" field.
  - Update .agnix.toml [rules] to allow enabling suppressed rules.
  - Update crates/agnix-lsp to respect the new state on config change.

  Risk: Touches config model; open as RFC-shaped issue first.

  Dependency: S047 and S049 ship at default-warn until #8 lands; re-classify as suppressed after.

  ---
  Summary

  8 issues spanning 49 confirmed-gap rules + 1 config-model feature + 1 doc drift fix.

  ┌─────┬────────────────────────────┬─────────────┬────────────┐
  │  #  │           Title            │    Rules    │ Difficulty │
  ├─────┼────────────────────────────┼─────────────┼────────────┤
  │ 1   │ rules.json line-limit fix  │ 0 (text)    │ XS         │
  ├─────┼────────────────────────────┼─────────────┼────────────┤
  │ 2   │ Agent desc quality         │ 3           │ S          │
  ├─────┼────────────────────────────┼─────────────┼────────────┤
  │ 3   │ CLAUDE.md canonical refs   │ 1           │ S          │
  ├─────┼────────────────────────────┼─────────────┼────────────┤
  │ 4   │ userConfig depth           │ 6           │ M          │
  ├─────┼────────────────────────────┼─────────────┼────────────┤
  │ 5   │ marketplace.json validator │ 7           │ M          │
  ├─────┼────────────────────────────┼─────────────┼────────────┤
  │ 6   │ Plugin hygiene bundle      │ 10          │ M          │
  ├─────┼────────────────────────────┼─────────────┼────────────┤
  │ 7   │ Skill content heuristics   │ 22          │ L          │
  ├─────┼────────────────────────────┼─────────────┼────────────┤
  │ 8   │ Default-suppressed tier    │ 0 (feature) │ L          │
  └─────┴────────────────────────────┴─────────────┴────────────┘
